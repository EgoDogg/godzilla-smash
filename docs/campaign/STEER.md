# STEER · human override channel

Append directives here; the run honors unprocessed ones at the next session-start ritual (and self-wake probes). Format: `## YYYY-MM-DD · PAUSE|SKIP|PIVOT|DEFER|NOTE · <target>` + body. Mark `[processed]` when folded in.

Use **NOTE** for feel feedback after a live deploy (G1) — e.g. "Nova Slam charge feels too slow." It queues here, non-blocking, and is folded into a tuning pass at the next version boundary.

---

## 2026-06-13 · NOTE · respawn pacing (G1 feel verdict on gz-v14) · [pending → U9 tuning]
Mike: "Faster respawns aren't needed. So many buildings/structures — I'd encourage SLOWER respawns to encourage more movement. Either that or fewer structures so faster respawns make sense."
→ OVERRIDES the research-locked U5 cut (RUBBLE_MS 6500→2800). In the dense city the dead-zone the research optimized against doesn't occur; fast respawn just lets the player camp. Human-arbiter wins. **RESOLVED 2026-06-13: Mike picked BOTH levers — slightly slower respawn AND a somewhat thinner city — and deferred the exact handling/values to the research's ≥97% recommendation** (workflow wf_60ad6a93-5f1). Folds into the gz-v15 deploy (new respawn-density-tuning unit). Live gz-v14 keeps fast respawns until then (async-queue by design).

## 2026-06-13 · NOTE · desktop/web control parity (G1, review item) · [pending → new unit]
Mike: "I played via HTML preview. I can't see how to use the upgrades via my PC. Review and unify so game actions have parity and are clear and obvious for both iOS and web."
→ Confirmed gap: `render.js drawTouchControls` early-returns when `Input.isTouch===false`, so on desktop there are NO visible SMASH/JUMP/NOVA discs. Desktop input exists (Space/left-click=attack, Shift/J=jump, F=charge Nova) but is UNDISCOVERABLE, and the Nova charge/cooldown ring only draws on the touch disc (the U3 "desktop F-charge feedback = glow only" v1 limitation). Needs a cross-platform control-affordance unit (clickable discs on desktop and/or a key legend + desktop Nova indicator). **RESOLVED 2026-06-13: Mike deferred the approach to the research's ≥97% recommendation** (workflow wf_60ad6a93-5f1, weighing discs-on-desktop vs key-legend vs auto-detect hybrid + a desktop Nova charge/cooldown indicator). New campaign unit "UP-parity" to be slotted into v16 (ships with gz-v16), filled from the research.

## 2026-06-13 · NOTE · TESTER QA GUIDANCE — four new work-streams (Mike-confirmed) · [pending → new units, two research/plan workflows running]
A tester gave 5 terse notes; clarified with Mike to 99% + two dynamic workflows. Decisions:
1. **Bounded economy + WIN FINALE** — the damage upgrade should be priced to its damage (Mike's start: cost = new total attack power; I have OVERRULE authority per research) and CAPPED so that maxing it = one-shot the strongest building (ROW_HP[18]=1e9). Reaching one-shot-1e9 = a **win celebration + the damage upgrade hard-stops** ("limit of upgrading"). Hard problem (forms cost up to 100e9 >> 1e9 finale; claws ×2/lvl unbounded today) → **research workflow wf_45dd62aa-cc7 running** (pricing + cap + claws/forms reconciliation + finale mechanic). → future unit **UE (economy-finale)**.
2. **FLYER FLIGHT** — Mothra + Rodan (flyer archetype) now ALWAYS HOVER at altitude: NO ground jump (jump disc hidden for them; jump stays for ground kaiju Godzilla/Ghidorah/Mecha), they **fly freely OVER buildings** (ignore ground collision), and being airborne they naturally reach the airplanes. Clear + implementable → future unit **UF (flyer-flight)** (entities locomotion/jump gate + render + input disc gating). No research needed.
3. **ALL-CHARACTER VISUAL REDESIGN** — full procedural-art redesign of all 20 forms (more distinct/recognizable/MonsterVerse-faithful at small iso scale; flyers get a proper airborne flight pose). Mike: "use up to 20 Opus 4.8 dynamic agents to plan fully" → **planning workflow wf_162f2875-d91 running** (→ docs/visual-redesign-plan.md). Implementation = a LARGE future phase (rewrite buildWyrm/Flyer/Hydra/Mecha for 20 forms) → unit set **UV\*** once the plan lands.
4. **Scope note:** these expand the campaign well beyond gz-v14/v15/v16. On workflow completion: write the result docs, add UE/UF/UV* units to campaign-progress.json, re-sequence, and present Mike a revised roadmap. The v16 batch (U9 consolidations, U10 sprite-geom, U11 dpr-sky, U12 facing-unify, UP parity, U13 airborne) may re-order around the redesign (esp. U12 facing-unify + the kaiju art interact). DO NOT start v16 visual units until the redesign plan lands (conflict risk).
