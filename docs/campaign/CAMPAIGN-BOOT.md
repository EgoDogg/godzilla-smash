# CAMPAIGN-BOOT · Godzilla Smash — gz-v14→v22 campaign (CLOSED) + POST-QA phase gz-v23→v28 (LIVE)

**The "where am I?" doc. Read first on every session boot.** ≤130 lines.
Plan: `docs/next-run-execution-plan.md` · Branch: `main` · Anchor: `3a81b40` (pre-campaign tip).
Repo: `/Users/MGitk/Projects/Godzilla Game/` · Live: https://egodogg.github.io/godzilla-smash/ · `EgoDogg/godzilla-smash`.

## SESSION-START RITUAL (in order)
1. **HALT-check** `docs/campaign/campaign-progress.json._halt`. If non-null → read the matching `docs/campaign/BLOCKERS.md` envelope before anything.
2. `git -C "/Users/MGitk/Projects/Godzilla Game" log --oneline -8 && git status --short && git tag --list 'gz-*'`
3. Read `docs/campaign/STEER.md` (human override: PAUSE/SKIP/PIVOT/DEFER/NOTE + queued feel feedback). Honor unprocessed directives first.
4. Read `docs/campaign/BLOCKERS.md` — pick up any block whose `### Resolution` is populated.
5. Read `docs/campaign/campaign-progress.json` → first unit in `_run_policy.sequence` with `passes:false` = resume point.
6. **Re-ground gate (MANDATORY):** before executing the resumed unit, run an inherit-session-model re-ground workflow that refutes the unit's plan scope vs current HEAD (anchors were checked at gz-v13; they drift as units land). If scope changed, re-find before fixing.
7. **Verify-green baseline:** `node --check js/*.js` clean; load the preview clean (cache-bust per the gotcha memory) before applying new edits.
8. Echo a 3-line summary: phase/unit · last tag · next action.

## MIKE GATE QUEUE (async · the run NEVER idles on a gate while un-gated work exists)

| # | Gate | Human does | Unblocks | Run does meanwhile | Status |
|---|------|------------|----------|--------------------|--------|
| G1 | Feel verdict per live deploy (gz-v14/v15/v16) | Play on iPad, reply with feel notes | A tuning pass at the next version boundary | Keeps driving the next units; feedback queues in STEER.md | ⏳ |
| G2 | D1 tier-rows design pick | Choose `street:1` vs `tier=floor(row/prow)` | The deferred density fix (own future run) | Everything else — D1 is the only gated unit | ⏳ |

G1 is **non-blocking** (continuous mode). No other gates.

## OPERATING MODE
- **Session policy · CONTINUOUS.** Durable state (JSON + this doc + tag) lands at EVERY unit; harness summarization carries context; the post-clear prompt is crash-recovery ONLY. Context heaviness is never itself a reason to stop.
- **Model · ONE exclusively** (`claude-opus-4-8[1m]`). Every `agent()` inherits it — omit `model`, no pins, no downgrade. FM-1 = inherit → (tighten schema · reduce width · backoff-retry) same model → abort+restart. Effort = the session-global Ultracode dial.
- **FULL ULTRACODE POOLS.** Every non-trivial unit = re-ground → design → 3-lens adversarial verify (ALWAYS) → 3-vote → implement → 5-gate; capstone loops til 2 dry (cap 4). Solo only for trivial mechanical edits. High-stakes units: U3 nova-slam, U9 consolidations, U12 facing-unify.
- **Cleanup authorized** — deletion/consolidation of dead+duplicated code is requested work (C1-C3, dead keyJump, etc.).
- **Budget governor.** Ultracode on (cost not a constraint). Park at a clean tag boundary on quota near-exhaustion; never START a unit that can't finish before the wall.
- **Self-wake parking.** When un-gated work is exhausted, ScheduleWakeup long-fallback (~1800-3600s) carrying the resume prompt; each wake probes gates + re-reads STEER/BLOCKERS. SUSPEND while quota near-exhausted.
- **Dispatch notifications.** One consolidated PushNotification per stopping point (deploy live · park · HIGH blocker).
- **Model-behavioral tuning:** report findings with confidence+severity · explicitly trigger subagents/cleanup/memory · pick-and-note on minor choices · silence-default narration · authorize cleanup explicitly.
- **TRUE STOP CONDITIONS (exhaustive):** ① U1..U13 shipped + self-wake pending (park) · ② HIGH security finding · ③ a human-locked pick that looks wrong (BLOCKERS HIGH + pivot) · ④ quota near-exhausted → park + resume on refresh · ⑤ final pre-ship review after gz-v16. **Nothing else stops the run.** Each session summary carries a one-line stoppage audit.

## PER-COMMIT GATE (all green; one finding/tight-cluster per commit)
1. `node --check` every edited `js/*.js` · 2. preview cache-bust + console clean · 3. behavior eval-drive vs LIVE objects (or screenshot for visual units) · 4. named regression checks · 5. (deploy units) `sw.js CACHE`===`config.CACHE_VERSION` + JSON/boot updated in the ship commit. Commit cites finding-id/unit + canon + best-practice; ends with `Co-Authored-By: Claude <noreply@anthropic.com>`. Never `--force`/`--amend`/`--no-verify`. Rollback = `git reset --hard <prev-tag>` (FM-2), never fix-forward. Flip `campaign-progress.json` (passes:true + evidence+tag+SHA) in the commit AFTER the fix.

## CHANGELOG (newest first · the top entries are the LIVE anchor)

### ░░ POST-QA PHASE (2026-06-14 · Mike's "What to review" feedback reopened the campaign) ░░
- 2026-06-14 · **SAVE-SYSTEM PHASE 2 (built+verified, NOT yet shipped) — tag `gz-save-p2`** — slot core API in economy.js (no UI; the Phase-3 Saves tab drives these): `listSlots()` ({id,name,active,summary{forms X/20, power, lastPlayed}} via a pure `powerOfGame(game)` that mirrors attackPower without touching live state) · `switchSlot(id)` (flush current slot → swap activeSlot → applySlotToState → `World.spawnCity()`+`Main.rebuildPlayer()`+refreshShop) · `newSlot(name)` (capped at Cfg.SAVE.SLOTS=3, name-trimmed, not auto-active) · `renameSlot` · `deleteSlot(id)` (non-active → just remove; active → switch to most-recently-played remaining; LAST → reseed a fresh slot + reset state) + session-only `_lastDeleted` undo (`undoDelete`/`canUndoDelete`). `sanitizeName` keeps unicode/emoji, strips control chars (charCode filter), clamps 24. Cache stays gz-v32. Verified on fresh port 8131: **29/29 behavioral tests** (list/summary · newSlot cap-refuse · switch flush+swap+reinit+noop/bad-id guards · rename empty-refuse/emoji · delete non-active/active/last + undo restore/clear). **NEXT = Phase 3 (Saves UI = a 5th shop tab, buildSaves reusing itemRow/.shop-item, two-step inline confirm).**
- 2026-06-14 · **SAVE-SYSTEM PHASE 1 (built+verified, NOT yet shipped) — tag `gz-save-p1`** — v4 multi-slot container foundation per `save-system-plan.md`. economy.js: single atomic localStorage key `godzilla-save-v4` = `{v:4, rev, activeSlot, migratedFrom?, slots:[{id,name,createdAt,lastPlayed,lastBackupAt,backupPromptSeen,prestige:0, game:{15 flat fields}}]}`; the shared **allowlist** `sanitizeGame(raw)` (reads only the 15 known fields by name, clamps each → prototype-pollution-proof) + `applySlotToState`/`serializeState`/`freshGame`/`freshContainer`/`readContainer`/`writeContainer`(monotonic rev)/`activeSlotOf`(self-heal). **Crash-safe v3→v4 migration**: wrap v3 as slot 1 (or fresh container for a new player), KEEP the v3 key, reap it only on a LATER boot that re-read a well-formed v4 (`maybeReapV3`, idempotent). `save()`/`load()` rewired to the container; the gz-v26 rebalance toast preserved across migration. config.js: `Cfg.SAVE`. **NOT a deploy — cache stays gz-v32** (ship = Phase 6; phases 1-5 commit without a cache bump so returning PWA users stay on the complete gz-v32 build, fresh visitors get the backward-safe migration). Verified on fresh port 8130: **39/39 behavioral tests** (migration preserves all progress + writes v4 + keeps v3 → 2nd boot reaps v3 · round-trip · fresh-player no-write-at-boot · corrupt-v3→fresh-kept · pre-formula toast acking · sanitize allowlist + no proto-pollution · dangling-slot self-heal · direct-v4 load) + clean-boot integration. **NEXT = Phase 2 (slot core API: listSlots/switchSlot/newSlot/renameSlot/deleteSlot+undo).**
- 2026-06-14 · **gz-v32 FIDELITY-MECHA (LIVE) — 🏁 STREAM A CHARACTER-FIDELITY COMPLETE (all 5 families)** — Mechagodzilla ×4, from `fidelity-plans-all.json` Mecha plan + a 3-lens adversarial design-verify (GO-WITH-ADJUSTMENTS; the BLOCKING item was M3 cyb=false visor byte-identity → protected the mecha_ghidorah cyborg head). Shipped: **mecha_3** red self-illum rim (palette `rimGlow` + shape `selfIllum`; `drawMechTorso` rebuilt as a Path2D, re-stroked with rimGlow @lw2.5/α0.5 — the near-black body now lifts off the night sky, the family's #1 failure) + concentrated **gapGlow** (drawMechPanels 6th `opts` param: 2 hot #ff2020/#ff6a4a seam channels + early-return that skips the body-wide rivets → "red through black" not a wash) + lean (bulk 1.0→0.9 + leanPose forward-shear `sh=BW*0.10` on the top verts only). **mecha_1** Cybanek temple-orbs separated from a NARROWED+centered visor (cyb-gated; cyb=false branch kept byte-identical) + orb radius floor 2.2→2.6/0.13→0.15 + bulbous `roundShoulders` arc caps + bulk 0.95→1.0. **super_mecha** Kiryu `headBlade` skull-ridge + `wiring` black cables + antennae 2→0. **mecha_2** gunmetal palette desat (#4a86bd→#5c6e84 etc, eye kept) + shoulder pod pushed out (px 0.40→0.50, pw 0.30→0.34). All new blocks save/restore-wrapped (no ctx-state leak). DEFERRED: super organicSeams + leadFin-trim, mecha_2 far-pod, mecha_3 missile-pods (the plan's "drop first" items). 5-gate: node --check · render-verify fresh port 8129 · console clean · **mecha_ghidorah cyborg center head + mecha_2 helmet visor verified UNCHANGED** · drift. Composite `shots/mecha-row-v32.png`. Cache gz-v31→**gz-v32**.
- 2026-06-14 · **gz-v31 FIDELITY-RODAN (LIVE)** — completes the Rodan pteranodon family (3 forms). From `fidelity-plans-all.json` Rodan plan + a 3-lens adversarial design-verify (GO-WITH-ADJUSTMENTS). Shipped: (1) **killed the magma-trunk** — `drawFissures` gains a `flyerMode` 8th param (sy clamped to the body band `[-0.66,-0.46]BH`, len 0.06–0.11·BH, ang horizontal, lw 3.4/2.2); buildFlyer:657 passes `true`, buildWyrm:441 stays 7-arg → **byte-identical wyrm cracks** (verified: burning/supernova vertical body fissures unchanged). (2) **wired the DEAD `drawCrestHorns`** into `drawFlyerHead`'s pteranodon branch — straight bone `#d8b070` (t1) / fork rock `#9a6a50`+dark-backing (t2) / fork **gold** `#ffd24a`+dark-backing (t3); the t2 `#9a6a50` is the verify pool's legibility fix (skinLight #8a2e1c was contrast 1.51, invisible). (3) **black beak** — `shape.beakColor||pal.skinDark`, `beakColor:'#1a0e08'` on rodan_fire (the #1 Fire-Rodan cue) + lengthen/droop (tip `BW*0.58`, clamped from 0.62 per the bounds verify — 0.62 hit screen_x 148.1). (4) `drawPteranoWing` trailing flatten (`+BH*0.10→0.04` ×2). (5) `drawPteranoBody` narrow (`hw 0.72→0.55, hh 0.15→0.12`) so wings dwarf the body. (6) NEW `drawChestSpikes` (bone/rock/gold sternum, pteranodon-gated, self-contained). E7 WH-cap raise DEFERRED (bounds). Render-verified on fresh port 8128 (drawFissures.arity=8 confirms load); composite `shots/rodan-row-v31.png`; moth head untouched. Cache gz-v30→**gz-v31**.
- 2026-06-14 · **gz-v30 FIDELITY-GHIDORAH (LIVE)** — drooping bat-wings (all 5 forms) + void_ghidorah no-body (necks from portal) + center-last trident z-order. (Shipped tag gz-v30; logged here for record.)
- 2026-06-14 · **gz-v29 FIDELITY-MOTHRA (LIVE)** — Mothra within-row escalation (threaded shape into drawMothThorax/drawFlyerHead; gxk joints/third-eye, supernova god-rays + level pose + aura, 4-wing separation, antenna recolor). (Shipped tag gz-v29; logged here for record.)
- 2026-06-14 · **gz-v28 FIDELITY-WYRM2 (LIVE)** — completes the Godzilla family fidelity. supernova body darkened `#251440→#0d011a` (AO floor `#2a1448`) + `plateHot:['#2a1448','#dcb0ff']` crystalline plate gradient (was a "flat purple lizard" → near-black over-charged mass with a towering bright-violet crystal spine); gvk eye `#ffcf4a→#cfcfcf` (last warm pixel on a cool-only form); gz2014 skinDark `#1e1e1e→#2b2b2b`. From the 3-lens wyrm re-plan. Cache gz-v27→**gz-v28**.
- 2026-06-14 · **gz-v27 FIDELITY-WYRM (LIVE)** — Godzilla silhouette ladder + palette. Fixed two inversions (gxk now leaner than gvk via torsoWidth 0.90/bulk 0.95; supernova out-towers gxk via plateHeightMul 1.18→1.38 + bulk 1.13); burning lead-plate (opt-gated `drawPlates` `leadPlate` + plumb) + ridge escalation; gz2014 pale eye; burning hot-at-base plates; gvk neutral charcoal. Cache gz-v26→**gz-v27**.
- 2026-06-14 · **STREAM A fidelity fleet** — per-family IMAGE-grounded design fleet (5 sub-fleets × 3 lenses + adversarial synth = 20 agents, vs the live 20-form composite `docs/campaign/shots/forms-current-composite.png` + reference specs). Rate-limited on first run (only wyrm completed) → resumed for the rest. All 5 family plans saved: `docs/campaign/fidelity-plans-all.json`. Wyrm SHIPPED (gz-v27/v28); **Mothra / Ghidorah / Rodan / Mecha plans READY, NOT implemented.** Reference specs: `kaiju-reference-specs.json`; gap-analysis: `fidelity-gaps-raw.json`.
- 2026-06-14 · **gz-v26 FORMSAXIS (LIVE)** — forms are now a DISTINCT power axis (Collection Multiplier + Option B). `attackPower = START_ATTACK × CLAWS_MULT^claws × (1 + Σ FORM_BONUS[owned] + FORM_BONUS[active])`; `clawsCost` carries the SAME factor → it CANCELS → bounded invariant (cost===2×power) holds; no unlock re-pricing, **NO save migration (stays v3)**. `FORM_BONUS` sums 63→×64 (config.js); memoized `collectionMult()` (recompute on buyForm/switchForm/load); shop X/20 ×mult meter + per-form contribution labels; game.js boot assert Σ===63; one-time "power rebalanced, progress intact" toast for pre-formula saves via `formAxisSeen`. 21-agent design fleet → `docs/campaign/forms-axis-design-2026-06.md`. Cache gz-v25→**gz-v26**.
- 2026-06-14 · **gz-v25 FINALE (LIVE)** — finale signpost (one-time max-power toast + render beacon `drawFinaleBeacon`: pulsing ring on-screen / clamped edge-arrow→statue, gated on canFinale); World 2 free auto-grant at finale (`markFinale` sets world2Unlocked, buy button removed); 🏆 re-viewable win-card button after finaleSeen. Mike's 3 TASTE picks. Cache gz-v24→**gz-v25**.
- 2026-06-14 · **gz-v24 LEGIBILITY (LIVE)** — wired the DEAD `FX.spawnRewardText` green combo +$ popup (fired from `world.destroy` so DoT/AoE kills show it too); form-purchase `Env.announce` fanfare; honest claws-hint + win-card copy; replaced the MISLEADING geographic "📍 Tier X/19" badge with a true "⚡ Power N/19" (count of ROW_HP rungs attackPower one-shots; 19/19 ⟺ canFinale). Stream C 8-lens progression swarm → `docs/campaign/progression-review-2026-06.md` (AUTO shipped, TASTE decided by Mike). Cache gz-v23→**gz-v24**.
- 2026-06-14 · **gz-v23 FLYERDEPTH (LIVE)** — flyers fly OVER buildings, not THROUGH (`pushPlayer` depthBias scaled by altitude `z·WZ/HH` capped 3; planes/buildings untouched, zero regression). Mike QA #1a. Cache gz-v22→**gz-v23**.
- VERIFICATION INFRA (2026-06-14): the **fresh-origin port trick** beats the chronic SW+HTTP-cache preview stick — add a NEW port to `.claude/launch.json` (8124, 8125, …) and `preview_start` it; a fresh origin loads current disk files clean. Per-family art composites via direct `A.build*` on a scratch canvas. Mike's 4 design questions answered via AskUserQuestion (art fidelity bar, Manhattan scope=bigger-restructure-OK, progression=analyze+auto-tune, forms-as-axis=ship-it).


- 2026-06-13 · **UD22 DEPLOY gz-v22 (LIVE) — ✅ CAMPAIGN 100% CLOSED** · tag `gz-ud22-v22-deploy` + D1 `gz-d1-tier-rows` — cache gz-v21→**gz-v22**. Ships **D1** (Mike's pick): `LAYOUT.street 3→1` so prow=3=tierRows → every HP tier gets exactly 2 building rows (even; was the uneven 2/1/1/2/0 cycle). Denser city (337 structures / 27.7% vs gz-v15's 192/16% — Mike's accepted trade). Math + live verified (all 20 tiers populated, game boots, denser even city renders). The autonomous campaign is fully complete: every build unit (UR..UD20) + 2 post-deploy audits (UH1/UH2/UH3) + the D1 design call, all live.
- 2026-06-13 · **UD21 DEPLOY gz-v21 (LIVE)** · tag `gz-ud21-v21-deploy` + UH3 `gz-uh3-final-review-fixes` — final pre-ship review hardening (true-stop #5 audit: 22 agents, 8 findings → 6 confirmed / 2 refuted). Cache gz-v20→**gz-v21**. UH3: flyers now HIT PLANES (the `footprintsNear` includeFlyers arg was missing in acquireTargets:1139 + fireDive:1307 — completes §7 'planes above AND ground below') + deleted the now-dead drawFlyerTorso/drawBirdLeg. Verified: 20/20 forms still bake, ground-targeting intact, console clean. Accepted (no fix, per audit): 2.5px tail clip + attack-frame mecha-laser overshoot. **Campaign fully closed after presenting D1 (Mike-gated).**
- 2026-06-13 · **UD20 DEPLOY gz-v20 (LIVE) — 🏁 CAMPAIGN COMPLETE** · tag `gz-ud20-v20-deploy` — cache gz-v19→**gz-v20** (both constants). The entire campaign is live: the full 20-form procedural art redesign (Godzilla ×5 · Mecha ×4 · Ghidorah ×5 · Mothra ×3 · Rodan ×3) + flyer-flight mechanics + airborne targeting, on top of the gz-v14→v16 feature/economy/parity work. Drift gate passed. **Remaining (post-build): FINAL PRE-SHIP REVIEW (multi-agent audit of the gz-v20 surface, true-stop #5) + present D1 (tier-rows) to Mike.**
- 2026-06-13 · FLYERMEGA flyer-finale · tag `gz-flyermega` — v20 finish-line mega-unit (mechanics + art, ONE commit). MECHANICS (entities/input/render/config): flyers (Mothra/Rodan, archetype 'flyer') HOVER at Cfg.FLYER_ALTITUDE, no ground jump, fly OVER buildings (skip collision); acquireTargets `permFlyer` flag → hit ground buildings AND planes; jump disc + legend Jump-row + fireJump gated off for flyers (ground kaiju unaffected). ART (archetypes/config): buildFlyer rewritten to the §7 airborne contract (drawAltitudeShadow, constant flap + hover, tucked talons, no legs/bob; drawMothThorax / drawPteranoBody); drawMothWing → four-wing + EYESPOTS; drawPteranoWing → scalloped + fireRim; Rodan magma via drawFissures; 6 forms re-palette'd (fixed the identical-cyan mothra bug; rodan cold→fire escalation). VERIFIED: behavioral (hover/fly-over/target-ground/ground-still-jumps) + LIVE (player-as-rodan renders flying by a plane, legend drops Jump) + composite (6 forms, no clip) — `docs/campaign/shots/flyermega-6forms.png`. Console clean. Cache stays gz-v19. **Next: UD20 = DEPLOY gz-v20 = CAMPAIGN COMPLETE + final pre-ship review + present D1.**
- 2026-06-13 · **UD19 DEPLOY gz-v19 (LIVE)** · tag `gz-ud19-v19-deploy` — cache gz-v18→**gz-v19** (both constants). Ships the redesigned **King Ghidorah family** live (ARTP4: tapered 3-neck fan, horn-crown heads, bat wings, gold/cyan cyborg, void wormhole, gold-token fix). Drift gate passed. G1 async (art fully-autonomous). **Next: FLYERMEGA (v20 finish-line: flyer flight + airborne targeting + Mothra/Rodan ×6 → UD20 = CAMPAIGN COMPLETE).**
- 2026-06-13 · ARTP4 ghidorah-redesign · tag `gz-artp4-ghidorah` — v19 art phase: the 5 King Ghidorah forms redesigned (visual-redesign-plan §4). 3 NEW shared helpers: `drawHydraNeck` (TAPERED FILLED neck via offset-fill — the garden-hose strokes are gone), `drawGhidorahHead` (skull + back-swept horn-crown via drawRidgeElement), `drawBatWing` (finger-spar membrane). `drawHydraHeads` rewritten with per-neck palette override → **mecha_ghidorah cyborg = gold organic outer necks + a steel/cyan machine center head (reusing the v18 drawMechHead)**. void_ghidorah = thorny cyan-rimmed necks + a wormhole. config: gold-token fix (dropped non-canonical purple) + per-form shape escalation. Verified: drawHydraNeck isolation test (tapers 17→10px) + composite of 5 forms × 2 facings (`docs/campaign/shots/artp4-ghidorah-5forms.png`); clip fixed; other families intact; console clean. Cache stays gz-v18. **Next: UD19 (deploy gz-v19).**
- 2026-06-13 · **UD18 DEPLOY gz-v18 (LIVE)** · tag `gz-ud18-v18-deploy` — cache gz-v17→**gz-v18** (both constants). Ships the redesigned **Mecha family** live (ARTP3: hard-block silhouettes, Cybanek orbs, shoulder cannons, Ghidorah head, backpack/lead fin, chrome/blue/black-red/gold). Drift gate passed. G1 async (art fully-autonomous). **Next: ARTP4 (King Ghidorah ×5 → UD19), then v20 flyers (FLYERMEGA → UD20 = finish).**
- 2026-06-13 · ARTP3 mecha-redesign · tag `gz-artp3-mecha` — v18 art phase: the 4 Mechagodzilla forms redesigned (visual-redesign-plan §6). `drawMechTorso` → HARD BLOCK (beveled trapezoid, flat-top shoulders) so the OUTLINE reads machine; RIM specular collapse (§1.1, pal.skinLight→rimCol); `drawMechHead` headStyle FINALIZED (box/roundShowa+Cybanek/helmet/ghidorah+horns — **v19 Ghidorah cyborg consumes 'ghidorah'**); `drawMechSpines` flat-top fins + dark core + spineSize/leadFin; NEW shoulder-cannon (mecha_2) + backpack (super_mecha) + drill tail (mecha_3). config: 4 shapes escalated (plates 5/7/10/11, bulk/spineSize per tier) + chrome/blue/black-red/gold palettes. Verified: composite of 4 forms × 2 facings → distinct machines (`docs/campaign/shots/artp3-mecha-4forms.png`); super_mecha clip fixed; mecha_3 night-visible; console clean. DEFERRED per §1.4 (portrait-scale): finger-missiles, xPanels, wiring, organic limb seams, angular mech limb routines. Cache stays gz-v17. **Next: UD18 (deploy gz-v18).**
- 2026-06-13 · **UD17 DEPLOY gz-v17 (LIVE)** · tag `gz-ud17-v17-deploy` — cache gz-v16→**gz-v17** (both constants). Ships the redesigned **Godzilla family** live (ARTP1 primitives + ARTP2: broken-slate ridge, magma fissures, self-illum rims, per-form bulk/plate escalation). Drift gate passed. G1 async (art fully-autonomous, no pre-deploy gate). **Next: ARTP3 (Mechagodzilla ×4 → UD18), then v19 Ghidorah, v20 flyers.**
- 2026-06-13 · ARTP2 wyrm-redesign · tag `gz-artp2-wyrm` — v17 art phase 2: the 5 Godzilla forms redesigned (visual-redesign-plan §2). `drawPlates` rewritten as a broken-slate ridge (`drawRidgeElement` dark CORE + lit FACE + `hash` jitter ±0.25·jag + plateJag razor-narrowing + per-form heightMul + broken battle-scar plates); `drawTorso`/`drawLeg`/`drawArm` parameterized WYRM-ONLY (narrow gvk, elephantine legs, wider arms, self-illum rim §1.2 — hydra/mecha byte-identical via defaults); `buildWyrm` data-driven (fissures on burning/supernova, lowered head). config: 5 shapes escalated (plates 10/10/11/13/16, bulk 1.05/1.05/1.0/0.98/1.08, plateJag 0.35→1.0) + rim/fissure/plateHot palettes + supernova night-vis. Verified: composite of 5 forms × 2 facings on night sky → distinct evolved silhouettes (`docs/campaign/shots/artp2-godzilla-5forms.png`); supernova top-clip fixed; other families intact; console clean. **DEFERRED (not a regression):** entities `drawGlow` is palette-only (N=9, no shape) → soft ridge-shimmer no longer per-plate-matches the new bake; reads fine; shape-aware glow = future unit. Cache stays gz-v16. **Next: UD17 (deploy gz-v17).**
- 2026-06-13 · ARTP1 art-primitives · tag `gz-artp1-primitives` — v17 art phase 1: shared house-style scaffolding in archetypes.js (visual-redesign-plan §1) — `rimCol(a)` RIM token (§1.1), deterministic `hash(i)` (§1.6), `drawRidgeElement` core-behind-face (§1.7), `drawFissures` (§1.7); all exported. Wired the 2 live house-rim literals to `rimCol` (drawTorso 0.22 / drawFlyerTorso 0.20 — render-identical). §1.6 bake guardrails documented. drawRidgeElement/drawFissures consumed by ARTP2+ (same-phase scaffolding). Empirically verified (eval-tests: core/face pixel-separation, fissure side/back + ≥2.2px stroke, hash determinism, RIM render-identity, buildWyrm/buildFlyer draw clean). No visible change. PREVIEW NOTE: archetypes.js is a chronic-stick module — use the gotcha's IIFE re-eval to verify. Cache stays gz-v16. **Next: ARTP2 (buildWyrm + 5 Godzilla forms).**
- 2026-06-13 · **DECISION (Mike) · art-redesign = FULLY AUTONOMOUS** — v17→v20 art deploys ship LIVE with no pre-deploy visual-approval gate; Mike reacts via STEER feel-notes. Recorded in STEER.md (locked). Art deploys (UD17–UD20) do NOT stop for visual sign-off; only the existing true-stops apply. Per-unit 5-gate screenshots still captured; deploy-time screenshot posted as non-blocking FYI.
- 2026-06-13 · UH2 audit-cleanup · tag `gz-uh2-audit-cleanup` — POST-UD16 audit cleanup (8 LOW). Removed dead CLAWS[] table (config) + dead keyJump (input); added clawsLevel load-clamp 0..64 + `START_ATTACK: 6` (latent NaN/Infinity guards); fixed stale grid-size comments in assets/iso/entities (64×32→56×28, cols/rows 12/19→21/58 — the U10 comment-trap + deferred U9 C3 sweep); corrected the `_wyrmHelpers` comment (export KEPT as the U12/art verification seam — deliberate deviation from the audit's delete-rec). Behavior-neutral (preview boot-clean; Cfg.START_ATTACK===6, CLAWS gone, clamp works). Ships at UD17. Audit refuted 2 (form-price tie, U12 single-table — both fine).
- 2026-06-13 · UH1 finale-pause-guard · tag `gz-uh1-finale-pause-guard` — POST-UD16 audit fix (MED). The win-finale card paused the sim but tracked no open-state, so the always-live ui.js onKeyDown (Esc/P/B/M) could un-pause / open the shop UNDER the visible modal. Fix: `state.winCardOpen` flag + onKeyDown guard (Escape/Enter→closeWinCard, swallow the rest) — mirrors the shop's pause ownership. Repro-verified: P/B swallowed (paused holds), Esc dismisses cleanly. Ships at UD17. **Post-UD16 audit: 28 agents, 11 findings → 9 confirmed (1 MED=UH1, 8 LOW=UH2), 2 refuted. No HIGH.**
- 2026-06-13 · **UD16 DEPLOY gz-v16 (LIVE)** · tag `gz-ud16-v16-deploy` — cache gz-v15→**gz-v16** (both constants: `sw.js` CACHE + `config.js` CACHE_VERSION; the gz-v15 comment annotations left as history). Drift gate passed (both gz-v16); ASSETS list current. Ships the full gz-v16 batch live: bounded economy (UE1) + win-finale (UE3) + 20-form re-pricing (UEFORMS) + KMP-prep consolidations (U9) + facing-unify (U12) + sprite-geom (U10) + dpr-sky (U11) + desktop parity (UP). G1 feel gate open (async). **Next: post-UD16 multi-agent audit pass (up to 30 Opus agents) over the gz-v16 surface, then art phase v17 (ARTP1→ARTP2→UD17).**
- 2026-06-13 · UP desktop-parity · tag `gz-up-desktop-parity` — **LAST gz-v16 unit done.** render.js `drawTouchControls` now branches on a new `Input.currentInput` ('touch'|'desktop', display-only, decoupled from the sticky `isTouch` routing flag) → `drawDesktopControls` paints a key-legend (SPACE/CLICK=Atomic Breath · SHIFT/J=Jump · WASD=Move · F=Nova Slam · SHOP↗=Upgrades) + surfaces the Nova charge/cd pip (drawFinisher reuse + 'F' badge + ready-flash). input.js seeds `currentInput` from `matchMedia(coarse && !fine)` (canon research-2026-06b:21/100 — the verify pool's HIGH caught my bare-coarse seed stranding hybrid laptops) and flips it to 'touch' on first touch/pen; isTouch routing untouched. Verified @1280×800: legend+pip render no-overlap (shot `docs/campaign/shots/up-desktop-parity.png`); touch mode still shows discs (no regression); ctx leak-free; console clean. Cache stays gz-v15. **Next: UD16 — DEPLOY gz-v16.**
- 2026-06-13 · U11 dpr-sky · tag `gz-u11-dpr-sky` — parallax sky now baked at device px (`round(lw*dp)x round(lh*dp)` via `c.setTransform(dp,..)`, mirrors assets.makeCanvas) so it's crisp on retina; `drawSkyLayers` rebuilds on `sky.d!==dp`, tiles by CSS `w` (not cv.width), 5-arg blit, and snaps `off=round(off*dp)/dp` to a whole device px (kills sub-pixel star/edge shimmer — added per the adversarial pool's one real finding). Runtime drawImage intercept @dpr=2 proved srcW=910 (device) + dx snapped to -24.5/-46.5/-79.5 + tiles abut; device-res shot `docs/campaign/shots/u11-dpr-sky.png` shows crisp stars/edges. Console clean. Cache stays gz-v15 (ships at UD16). Next: UP (desktop parity).
- 2026-06-13 · U10 sprite-geometry · tag `gz-u10-sprite-geometry` — sprites_special HW/HH 32/16→28/14 (matches assets GRID-derived; specials were 4px/tile too wide) + bakeSpecial footprint:{w:fw} key. Screenshot: statue+pyramid fit, no clipping. Cache stays gz-v15 (ships at UD16). Next: U11.
- 2026-06-13 · U12 facing-unify · tag `gz-u12-facing-unify` — facingGeom deduped to one table (entities→Archetypes, sameObject verified); 8 facings render; behavior-identical. Cache stays gz-v15.
- 2026-06-13 · **AUTONOMOUS-COMPLETION posture** (Mike) — drive to completion, NO pause-for-permission; self-wake loops (ScheduleWakeup 'continue the campaign', 1200-3600s, longer on rate-limit); audits with up to 30 Opus agents (post-UD16, each art phase, pre-UD20). Mirrored in `_run_policy._locked`.
- 2026-06-13 · U9 config-consolidation · tag `gz-u09-config-consolidation` — C1: scattered tunables → Config.LOCO/CAMERA/INPUT_GEO/AUDIO/RENDER (behavior-identical, fallbacks) + dead CLAWS_BASE/GROWTH removed. Preview-verified (values==old, focus/iso/locomotion unchanged). C2/remaining-C3 deferred as optional polish. KMP keystone done. Cache stays gz-v15 (ships at UD16). Next: U12.
- 2026-06-13 · UEFORMS reprice · tag `gz-ueforms-reprice` — all 20 forms re-priced <1e9 (config-only; gz2014 free → super_mecha 90M, monotone by base). Economy trilogy (UE1+UE3+UEFORMS) COMPLETE. Cache stays gz-v15 (ships at UD16). Next: U9 (consolidations).
- 2026-06-13 · UE3 win-finale · tag `gz-ue3-win-finale` — statue→1e9 + world.triggerFinale (climax FX + delayed 'CITY LEVELLED 100%' card) on statue-destroy-while-can-one-shot; economy finaleSeen/peakCombo/canFinale/finaleStats; #wincard overlay + ui.showWinCard (pauses, Continue=free-roam). Preview: fires once, stats render, close unpauses. Cache stays gz-v15 (ships at UD16). Next: UEFORMS.
- 2026-06-13 · UE1 bounded-economy · tag `gz-ue1-bounded-economy` — clawsCost=round(base·2^(L+1)) (back-compat L0=12) + buyClaws caps at attackPower>=1e9 + shop MAX-state/crossing telegraph. Preview: gz2014 caps at L28 (1.6B), high forms cap fast, all rows intact. Cache stays gz-v15 (ships at UD16). Next: UE3 (win-finale).
- 2026-06-13 · **CAMPAIGN RE-PLAN (gz-v16→v20)** — tester QA guidance expanded the run. Strategic workflow wf_e72f4a3c-ded (conf 0.97) sequenced all remaining work into 5 deploys: **gz-v16** = bounded economy + win-finale (UE1/UE3) + form re-pricing (UEFORMS, all 20 <1e9) + consolidations (U9) + facing-unify (U12) + sprite/sky fixes (U10/U11) + desktop parity (UP); **gz-v17** = art P1 primitives + P2 Godzilla; **gz-v18** = art P3 Mecha; **gz-v19** = art P4 Ghidorah; **gz-v20** = flyer mega-unit (flight+airborne-targeting+art P5 Mothra/Rodan). Specs: docs/campaign-roadmap-v16plus.md + research-2026-06b/06c.md + visual-redesign-plan.md. 29 units total, 11 done, resume=UE1.
- 2026-06-13 · **UD15 DEPLOY gz-v15 (LIVE)** · tag `gz-ud15-v15-deploy` — cache gz-v14→**gz-v15**. Ships the P0 stuck-autofire fix + PWA hardening (sw fetch handler now active) + save system + the respawn movement fix (off-screen gate + slower/thinner). Moved ahead of U9 (consolidations → v16) to get user-facing value out fast. G1 gate open: respawn feel. Preview-verified + live sw.js polled gz-v15.
- 2026-06-13 · UT respawn-move · tag `gz-ut-respawn-move` — Mike G1 fix (he chose gate+config): OFF-SCREEN RESPAWN GATE (cleared blocks refill only once off-screen, via camera.cull AABB+margin, headless-guarded) + RUBBLE_MS 2800→4500 / PER_TIER 150→250 + LAYOUT street 2→3/streetW 3→4 (270→201 structures, income-neutral). Preview-proven: on-screen=holds rubble, off-screen=respawns. Feel G1-tunable. Cache stays gz-v14.
- 2026-06-13 · U8 save-system · tag `gz-u08-save-system` — lastSeen timestamp + dirty-flag save cadence (bankDestroy/frontier batched, flushed 2s + page-hide; purchases immediate). Preview: 10 kills → 0 writes, flush → 1, purchase immediate, blob has v:3+lastSeen. Cache stays gz-v14.
- 2026-06-13 · U7 pwa-hardening-trio · tag `gz-u07-pwa-hardening` — sw.js fetch handler (HTML fallback only for navigations, 503 else, OK-only caching) + cache-version cross-comments + game.js boot drift-warn + economy safeSave-failure toast (once). Preview-verified all three; SW runtime ships at U9. Cache stays gz-v14.
- 2026-06-13 · U6 input-stuck-fixes · tag `gz-u06-input-stuck` — P0 `spaceHeld=false` in clearAllHeld (stuck-autofire-after-blur) + `mouseAtkPointerId` branch in onPointerCancel. Preview-verified the P0 repro (Space→blur→autofire stops). Cache stays gz-v14 (ships at U9).
- 2026-06-13 · **U5 DEPLOY gz-v14 (LIVE)** · tag `gz-u05-v14-deploy` — balance tuning (COMBO 3250/0.12, RUBBLE 2800+150/tier, hit-stop 60-95ms) + cache bump gz-v13→**gz-v14**. **v14 phase COMPLETE**: Attack-Speed + Move-Speed tracks, Nova Slam finisher, final touch geometry, tuning — all live. G1 feel-verdict gate now OPEN (async). Preview-verified combo ramp +0.12/destroy; live sw.js polled gz-v14.
- 2026-06-13 · U4 touch-geometry · tag `gz-u04-touch` — input.js: JOY_DEADZONE .18→.12, LEFT_ZONE .55→.42 (+ deleted dead RIGHT_ZONE), SMASH_MIN→96, JUMP_MIN→76, JOY_RADIUS kept 70; NEW safe-area-inset probe in layoutSmash. Controls FINAL for first play. Preview-verified: L-shape + clearance hold, safe-area probe present, console clean. Cache still gz-v13.
- 2026-06-13 · U3 nova-slam-finisher · tag `gz-u03-novaslam` — Nova Slam across 6 files (config/economy/entities/input/render/audio): charged AoE disc, `fireFinisher` parallel to startAttack (autofire untouched), NOVA disc L-shape left of SMASH + F-key, charge/cooldown ring. Preview-verified: charge→release fires (cd=8, AoE hit 4 buildings), atkCooldown independent, move-slow ×0.5, silent-cancel, geometry both orientations, screenshot confirms 3 discs, shop row. Cache still gz-v13.
- 2026-06-13 · U2 move-speed-track · tag `gz-u02-movespeed` — `Config.MOVESPD` + cached `moveSpeedMult()` in economy + entities scales accel/maxSp/walkPhase. Preview empirical drive: terminal-speed ratio L0→max = 1.587 (=cap); costs 40/96/230/553/1327/3185; shop 'Titan Stride' row. Cache still gz-v13. (Gate caught a missing Config.MOVESPD def — fixed pre-ship.)
- 2026-06-13 · U1 atk-speed-track · tag `gz-u01-atkspeed` — `Config.ATKSPD` + asymptotic `attackGateFor` reduction + `buyAtkSpeed`/cost/shop-row/save in economy.js. Preview-verified: gz2014 gate 0.126→0.081s (×1.56) at L6, costs 10/32/102/328/1049/3355, cap enforced, L0 unchanged. Cache still gz-v13.
- 2026-06-13 · UR research-lock · tag `gz-ur-research-lock` — `docs/research-2026-06.md`; 23 params (17 locked≥.97 + 6 fallbacks); apply-spec drives U1-U5. Refinements: RUBBLE_PER_TIER 450→150, hit-stop cap 95, combo window 3250, + safe-area insets.
- 2026-06-13 · campaign bootstrap · anchor `3a81b40` — durable state scaffolded.

## RESUME ANCHOR (next concrete action)

### ░░ CURRENT — POST-QA PHASE (2026-06-14) · gz-v32 LIVE · STREAM A FIDELITY COMPLETE ░░
The campaign closed at gz-v22; Mike's "What to review" QA (4 asks: flyer-through-buildings, art-fidelity vs real refs, Manhattan city, progression earned-vs-dopamine) **reopened it**. SHIPPED + LIVE this phase: gz-v23 (flyer depth) · gz-v24 (legibility + Power-meter) · gz-v25 (finale signpost / World-2 / win-card) · gz-v26 (forms-as-axis) · gz-v27+v28 (Godzilla fidelity) · gz-v29 (Mothra fidelity) · gz-v30 (Ghidorah fidelity) · gz-v31 (Rodan fidelity) · gz-v32 (Mecha fidelity). **🏁 Stream A character-fidelity is now COMPLETE — all 5 families (Godzilla/Mothra/Ghidorah/Rodan/Mecha, 20 forms) refined per `fidelity-plans-all.json`.** Live: https://egodogg.github.io/godzilla-smash/ · clean tree, local===remote.

✅ **Stream A fidelity (all shipped):** Godzilla gz-v27/v28 · Mothra gz-v29 · Ghidorah gz-v30 · Rodan gz-v31 · Mecha gz-v32. (Per-family change-detail in the changelog above + `fidelity-plans-all.json`.)

**NEXT — ← RESUME HERE: SAVE SYSTEM (gz-v33+)** per `docs/campaign/save-system-plan.md` (decisions-LOCKED, Mike's 4 build choices). Backend-free multi-slot localStorage `godzilla-save-v4` container (single atomic key) + IndexedDB anti-loss mirror + export/import `GZS1:` codes (base64url + crc32) + a 5th "Saves" shop tab. **6-phase rollout, each verified on a fresh origin:** (1) v4 container + v3→v4 migration + the shared allowlist `sanitizeGame(raw)` (lift the body of economy.js `load()` verbatim into a pure allowlist build); (2) slot core API (string-id slots, listSlots/switchSlot/newSlot/renameSlot/deleteSlot+undo); (3) Saves UI tab (buildSaves reuses itemRow/.shop-item; two-step inline confirm, never window.confirm); (4) export/import (U.crc32 + U.b64u in utils.js, clipboard/paste, live pre-validate); (5) anti-loss (IDB mirror + asyncIdbRecover + persist() gated on first engagement + passive backup banner); (6) ship = bump sw.js CACHE + CACHE_VERSION together. Mike's LOCKED decisions: persist gated-on-engagement · new+confirm-overwrite import · delete-undo · passive-banner-only (no nag prompts). All new logic folds into economy.js (+ utils.js) — NO new file (dodges the sw.js ASSETS cache gotcha). Schema stays slot-internal = today's flat v3 shape minus `v`. **THEN — Stream B (Manhattan city restructure, Mike #2 — bigger-restructure + save-migration sanctioned) + the lower-stakes progression TASTE items (`progression-review-2026-06.md`).**

Render-verify each via direct `A.build*` composite (chronic-stick: IIFE re-eval config/archetypes; or a fresh-origin port). Ship live per the deploy ritual.

**THEN — Stream B Manhattan city restructure (Mike #2, NOT started):** bigger-restructure + save-migration sanctioned. Research swarm → rework `spawnCity`/`LAYOUT`/`GRID` (long rectangular blocks, varied footprints/heights, park-plaza breaks, re-derive depth→tier mapping) → autonomous close-in loop (generate→screenshot→critique→adjust). C's density/pacing output feeds it.

**PLUS — lower-stakes progression TASTE items** (`progression-review-2026-06.md`): utility-track repricing, onboarding nudges, collection-counter/tier-up fanfare, combo-window grace — documented, ship low-risk on Mike's word.

**PLUS — SAVE SYSTEM (planning, 2026-06-14):** Mike requested a save-system plan; scope being gathered via questions. Current = localStorage `godzilla-save-v3` (formAxisSeen added gz-v26; warns once on quota/eviction). Plan → `docs/campaign/save-system-plan.md` (TBD).

⟨history below — the gz-v14→v22 campaign, now closed; superseded by the POST-QA phase above⟩

**✅ CAMPAIGN 100% CLOSED — gz-v22 LIVE. Nothing remains.** Every unit shipped: UR (research) → U1..UT (gz-v14/v15 features+hardening) → UE1/UE3/UEFORMS/U9-U12/UP/UD16 (gz-v16 economy+finale+parity+KMP-prep, post-deploy 28-agent audit → UH1/UH2) → ARTP1-2/UD17 (Godzilla art) → ARTP3/UD18 (Mecha) → ARTP4/UD19 (Ghidorah) → FLYERMEGA/UD20 (flyer flight + Mothra/Rodan) → final 22-agent pre-ship audit → UH3/UD21 (flyers-hit-planes + cleanup) → D1/UD22 (Mike's tier-rows pick: street:1, even tiers). The full 20-form procedural art redesign + flyer-flight mechanics + all gameplay/economy work is live at https://egodogg.github.io/godzilla-smash/. If the campaign is re-opened (new feedback/scope), this boot doc + campaign-progress.json carry the full state; otherwise the autonomous run is DONE. G1 feel-verdict queue (STEER.md) stays open for any playtest notes. The campaign's build phase is COMPLETE: all of gz-v14→v20 is live (features, bounded economy + win-finale, KMP-prep, desktop parity, the full 20-form art redesign, flyer flight). Two closeout items remain, both true-stops to hand to Mike: (1) **FINAL PRE-SHIP REVIEW** — a multi-agent audit (up to ~30 Opus agents) of the whole gz-v20 surface, mirroring the post-UD16 audit; fold cheap confirmed fixes as their own tagged units, queue any HIGH to BLOCKERS. (2) **Present D1** — the deferred world-thin-tier-rows design call (street:1 vs tier=floor(row/prow)); present the options + tradeoffs, do NOT implement (Mike picks). After both: the autonomous campaign has reached its finish line. Art review = FULLY AUTONOMOUS (Mike, locked in STEER). FLYERMEGA = ONE combined commit (visual-redesign-plan §3/§5/§7 + UF/U13 mechanics; knot (a): mechanics merge INTO the art, never ship mechanics-only first): (1) **UF FLIGHT** — entities locomotion: Mothra+Rodan (flyer archetype) ALWAYS hover at altitude, NO ground jump (hide the jump disc for flyers in input/render; jump stays for ground kaiju), fly FREELY OVER buildings (ignore ground collision), `drawAltitudeShadow`. (2) **U13 airborne-targeting** — acquireTargets already has the airborne flag + gating (entities.js ~:1086-1163); make includeFlyers / honor-ground-tap-while-airborne correct under steady-airborne flyers. (3) **ART-P5 buildFlyer rewrite** — Mothra ×3 (four-wing FORE+HIND, per-wing eyespots, furred OVAL thorax via drawMothThorax) + Rodan ×3 (pteranodon, crest horns, magma cracks via the ARTP1 `drawFissures` as drawMagmaCracks) with the shared airborne flight pose + wing-flap off walkPhase. Verify: flyers hover + fly over buildings + hit planes above AND ground below; 6 forms screenshot; jump intact for ground kaiju. Then UD20 (deploy gz-v20 = CAMPAIGN COMPLETE) + FINAL PRE-SHIP REVIEW (true stop) + present D1 (tier-rows design call). Authoritative resume = first `passes:false` in `_run_policy.sequence`. PREVIEW: archetypes.js/config.js/entities.js chronic-stick — IIFE re-eval per the gotcha; render via direct A.buildFlyer. DEFERRED polish queue: shape-aware entities drawGlow (palette-only N=9); mecha/ghidorah sub-2px detail; void 'no-body' + back-facing neck-stubs.** AUTONOMOUS-COMPLETION mode (Mike): do NOT pause for permission — after each unit/clean-tag, ScheduleWakeup ('continue the campaign', 1200-3600s; longer on rate-limit) to self-resume; surface only on a true-stop. NEXT: (1) **post-UD16 multi-agent audit pass** — a workflow (up to ~30 Opus agents, inherit model) auditing the whole gz-v16 surface (the 8 shipped units' code for correctness/regression/leftover-dead-code; confirm no economy/finale/parity bug slipped through); file any HIGH to BLOCKERS, fold cheap fixes as their own tagged units. (2) Then **ARTP1** (art shared primitives, visual-redesign-plan §1) → ARTP2 (Godzilla wyrm ×5) → UD17 (deploy gz-v17). Then v18 (Mecha), v19 (Ghidorah), v20 (flyer mega-unit + final pre-ship review = the true stop). Spec: docs/visual-redesign-plan.md. Authoritative resume = first `passes:false` in `_run_policy.sequence` (currently ARTP1; the audit is an inserted quality pass, not a sequence unit). ⟨superseded U9/U11/UP specs below⟩
**Resume point = U9 (consolidations — HIGH-stakes behavior-identical, KMP-prep). v15 LIVE; economy trilogy UE1+UE3+UEFORMS done.** U9 (full per-unit loop + capstone): C1 scattered tunables → Config sub-objects — `Config.LOCO {ACCEL 1060, MAX_SPEED 260, FRICTION 12, COLLIDE_R 18, WALK_SPEED 0.22}` (keep ÷PX_PER_TILE at the entities.js use site), `Config.CAMERA {focusX .5, focusY .66, SHAKE_MAX_PX 12, SHAKE_DECAY .02, SHAKE_TRAUMA_K 1/85}` (iso.js), `Config.INPUT_GEO {JOY_RADIUS 70, JOY_DEADZONE .12, SMASH_MIN 96, JUMP_MIN 76, FINISHER_MIN 76, LEFT_ZONE .42, FACE_AHEAD_PX 28, NEAR_TARGET_PX 40}` (input.js), `Config.AUDIO {MASTER_GAIN .5}` (audio.js), `Config.RENDER {CULL_PAD 3, CULL_RISE_ROWS 14}` (render.js — RENDER sub-object lands before U11/UP, no merge race since both shipped already). C2 single-source iso math (consumers read `G.iso.HW/HH/WZ`) + render.js per-frame `worldToScreen`→`worldToScreenInto` allocs. C3 stale-comment sweep + delete now-dead `CLAWS_BASE/CLAWS_GROWTH/CLAWS[]` (UE1 replaced them) + dead `keyJump` (input.js) + audio closed-ctx guard. Verify EACH moved value via live-object equality (`GAME.Config.LOCO.ACCEL===1060` etc.) — MUST be behavior-identical. Then U12 facing-unify → U10 sprite-geom → U11 dpr-sky → UP parity → UD16 (deploy gz-v16). Spec: docs/campaign-roadmap-v16plus.md + kmp-architecture-roadmap.md M0.** Full sequence in campaign-progress.json `_run_policy.sequence` (authoritative); per-unit specs in the JSON notes + docs/campaign-roadmap-v16plus.md + research-2026-06b/06c.md + visual-redesign-plan.md.
**gz-v16 batch order:** UE1 (clawsCost=round(base·2^(L+1)) + buyClaws cap at attackPower>=max(ROW_HP)=1e9) → UE3 (statue→1e9 + win-finale climax/card/capstone) → UEFORMS (re-price all 20 forms <1e9 per the roadmap table) → U9 (consolidations, RENDER sub-object before U11/UP) → U12 (facing-unify) → U10 (sprite-geom) → U11 (dpr-sky) → UP (desktop parity) → UD16 (deploy gz-v16). Then v17–v20 art per the roadmap. Each unit: full per-unit loop → 5-gate → tag → flip JSON → push; deploy units bump both cache constants + poll live. D1 (tier-rows) is the end-of-run Mike-gated design call. Authoritative resume = first `passes:false` in `_run_policy.sequence` (currently UE1).

**THE POST-CLEAR / CRASH-RECOVERY PROMPT — POST-QA PHASE (paste verbatim, 2026-06-14):**
*Resume the Godzilla Smash POST-QA autonomous run (gz-v32 LIVE; repo /Users/MGitk/Projects/Godzilla Game, live https://egodogg.github.io/godzilla-smash/). Read in order: (1) docs/campaign/CAMPAIGN-BOOT.md — the "CURRENT · gz-v32 LIVE · STREAM A FIDELITY COMPLETE" block + the two SAVE-SYSTEM PHASE changelog entries at the top; (2) docs/campaign/save-system-plan.md — the implementation-ready, decisions-LOCKED backend-free multi-slot save system (the active build); (3) docs/campaign/STEER.md + BLOCKERS.md. SECONDARY: docs/campaign/fidelity-plans-all.json (Stream A art plans — DONE), progression-review-2026-06.md (lower-stakes TASTE items). Run the SESSION-START RITUAL (HALT-check campaign-progress.json._halt; git log/status/tags gz-* + gz-save-*; STEER; BLOCKERS; re-ground gate). STATE: the gz-v14→v22 campaign closed; Mike's "What to review" QA reopened it. SHIPPED+LIVE: gz-v23 flyer-depth · gz-v24 legibility/Power-meter · gz-v25 finale-signpost · gz-v26 forms-as-axis · gz-v27→v32 = 🏁 STREAM A CHARACTER-FIDELITY COMPLETE (all 5 kaiju families / 20 forms). SAVE SYSTEM IN PROGRESS (committed to main, NOT yet shipped — cache deliberately stays gz-v32 until Phase 6): Phase 1 (v4 container + crash-safe v3→v4 migration + shared allowlist sanitizeGame; tag gz-save-p1; 39/39 tests) + Phase 2 (slot core API listSlots/switchSlot/newSlot/renameSlot/deleteSlot+undo; tag gz-save-p2; 29/29 tests) DONE. RESUME POINT = SAVE SYSTEM PHASE 3, then 4→6. Build EACH phase verified on a FRESH launch.json port (the SW-stick gotcha — bump the port each cycle: 8132, 8133, …): **P3** = Saves UI = a 5th shop tab (`<button data-tab="saves">Saves</button>` after Worlds, auto-wired by the generic data-tab delegation; shop auto-pauses); buildSaves(body) reuses itemRow()/.shop-item/.buy/.tag (NO new CSS), slot list + Switch/New/Rename/Delete with a TWO-STEP INLINE confirm (never window.confirm) + delete-undo + capacity footer; wire to the Phase-2 Economy API; screenshot-verify. **P4** = export/import GZS1: codes (U.crc32 + U.b64u Unicode-safe in utils.js; exportSlotCode/parseSlotCode/importSlotCode in economy.js; reject len>20000→charset→crc32→JSON.parse→sanitizeGame→new-slot-or-confirm-overwrite; clipboard copy/paste); behavioral round-trip + reject truncated/old/oversized. **P5** = anti-loss (IndexedDB mirror db 'godzilla'/store 'saves' debounced ~5s + forced on pagehide/purchase; asyncIdbRecover post-paint only when LS fresh/absent AND IDB.rev strictly higher; navigator.storage.persist() gated on first engagement; passive backup banner + durability chip green/amber/red — NO nag prompts). **P6** = SHIP: bump sw.js CACHE + config.CACHE_VERSION together to gz-v33 + poll live. Mike's LOCKED decisions: persist gated-on-engagement · new+confirm-overwrite import · delete-undo · passive-banner-only. All logic folds into economy.js (+ utils.js) — NO new file. THEN Stream B (Manhattan city restructure, Mike #2 — bigger-restructure + save-migration sanctioned) + lower-stakes progression TASTE. MODE: model claude-opus-4-8[1m] EXCLUSIVELY (every agent inherits, no downgrade, FM-1 = same-model backoff); FULL ULTRACODE (adversarial design-verify before high-risk units; behavioral tests on fresh origins for logic, screenshots for UI). Per-phase gate [node --check every edited js · fresh-origin verify · console clean · no regression to load/save/economy · cache INTENTIONALLY unchanged until Phase 6] → tag gz-save-pN → push (NO live poll until Phase 6). Surface to Mike only on a true-stop. If interrupted, resume at the first un-shipped save phase. ⟨older fidelity-era + gz-v16-era prompts below — superseded⟩*

**THE POST-CLEAR / CRASH-RECOVERY PROMPT (gz-v16-era · superseded):**
*Resume the Godzilla Smash autonomous campaign. Read in order: (1) docs/campaign/CAMPAIGN-BOOT.md (ritual + changelog top = live anchor); (2) the build-source docs — docs/campaign-roadmap-v16plus.md (the gz-v16→v20 sequence + form-cost table) + research-2026-06b.md (desktop parity) + research-2026-06c.md (bounded economy + finale) + visual-redesign-plan.md (art); (3) docs/campaign/campaign-progress.json (default-FAIL; sequence is authoritative); (4) docs/campaign/STEER.md + BLOCKERS.md. Model = `claude-opus-4-8[1m]` EXCLUSIVELY · every agent inherits · NO downgrade · FM-1 = same-model backoff → abort+restart · effort = Ultracode session dial. Every non-trivial unit runs FULL ULTRACODE POOLS: re-ground → design → 3-lens adversarial verify ALWAYS → 3-vote → implement → 5-gate + tag + flip JSON + push; capstone loops til 2 dry. Resume point = the first passes:false unit in _run_policy.sequence (currently ARTP1 — all gz-v16 units + the post-UD16 audit fixes UH1/UH2 shipped; gz-v16 is LIVE). Continuous + async feel-queue; G1 feel verdicts never block; deploy units (UD16/UD17/UD18/UD19/UD20) bump sw.js CACHE + config.CACHE_VERSION together + poll live; D1 (tier-rows) is the end-of-run Mike-gated design call (present, don't implement). If interrupted, resume at the first passes:false unit.*
